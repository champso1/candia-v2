option(BUILD_TESTS "Build the 'tests', which are effectively just a more exhaustive set of executables for testing the evolution." NO)
if (BUILD_TESTS)
  add_subdirectory(main)
  add_subdirectory(plot-generation)
  add_subdirectory(testing)
  add_subdirectory(lhapdf-datafile)
endif()
